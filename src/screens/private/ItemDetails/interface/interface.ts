export type RoutesParamsProps = {
  id: string;
};

export interface ProdutoProps {
  id: string;
  disponing: boolean;
  titulo: string;
  autor: string;
  category: string;
  sinopse: string;
  preco: number;
  img: string;
}
